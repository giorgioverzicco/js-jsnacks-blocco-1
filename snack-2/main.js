const firstWord = prompt("Scrivi una parola:");
const secondWord = prompt("Scrivi un'altra parola:");
alert(firstWord.length >= secondWord.length ? firstWord + " " + secondWord : secondWord + " " + firstWord);
